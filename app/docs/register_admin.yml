tags:
  - Admin
# summary: Admin Registration
operationId: registerAdmin
description: Registers a new admin user and returns a success message.
consumes:
  - application/json
produces:
  - application/json
parameters:
  - in: body
    name: body
    required: true
    schema:
      type: object
      properties:
        email:
          type: string
          example: johndoe@example.com
        password:
          type: string
          example: secretpassword
        confirm_password:
          type: string
          example: secretpassword
      required:
        - email
        - password
        - confirm_password
responses:
  201:
    description: User created successfully
  400:
    description: Validation error
  409:
    description: User already exists
  500:
    description: Internal server error
