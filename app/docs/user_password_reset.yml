tags:
  - User
operationId: resetPassword
description: Allows user to reset the password.
consumes:
  - application/json
produces:
  - application/json
parameters:
  - in: body
    name: body
    required: true
    schema:
      type: object
      properties:
        password:
          type: string
          example: secretpassword
        confirm_password:
          type: string
          example: secretpassword
      required:
        - password
        - confirm_password
responses:
  200:
    description: User created successfully
  400:
    description: Validation error
  401:
    description: Bad request
  500:
    description: Internal server error
