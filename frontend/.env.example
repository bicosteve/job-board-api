# Production (Vercel): full base path to the Flask API, without trailing slash.
# Example: https://job-board-api-example.onrender.com/v0/api
VITE_API_URL=

# Local dev: leave unset to use Vite proxy (see vite.config.ts) at /v0/api
